{% extends 'base.html.twig' %}

{% block title %}Créer une série - {{ parent() }}{% endblock %}

{% block body %}
    <h1>Création d'une nouvelle série</h1>

    {{ form_start(serieForm) }}

    {{ form_widget(serieForm) }}

    <button type="submit">Envoyer</button>

    {{ form_end(serieForm) }}

{% endblock %}